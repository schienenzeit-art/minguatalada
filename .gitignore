# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Datenbank & Laufzeitdaten — NIE einchecken (personenbezogene Daten / DSGVO)
data/*.db
data/backups/
data/documents/
data/pdfs/
data/excel/
data/updates/
data/logs/

# Auch Unterverzeichnisse explizit absichern
data/documents/**
data/pdfs/**
data/excel/**
data/backups/**
data/logs/**
data/updates/**

# Installer-Binaries (gross, kein Source-Code)
installer/*.exe

# Build-Artefakte
build/
dist/
*.spec.bak

# IDE
.vscode/
.idea/

# Node (nur für lokale Tooling-Tools)
node_modules/

# Windows
Thumbs.db
Desktop.ini

# Pytest
.pytest_cache/

# Code-Signing: privater Schlüssel NIEMALS committen
certs/*.key
certs/*.pfx
certs/*.p12

# Signing-Keys (neues Verzeichnis)
keys/private.pem
keys/*.key
keys/*.pfx

# Release-Artefakte (lokal, nicht in Git)
releases/
deploy/
